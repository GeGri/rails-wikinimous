<div class = "container" >
<div class = "col-sm-8 col-sm-offset-2">
<article>
  <% @articles.each do |article| %>
    <h2 class = "htwo"><%= link_to article.title, article_path(article) %></h2>
    <p><%= article.content%></p>
  <% end %>
</article>
</div>
<p><%= link_to "Add a new task", article_path('new') %></p>
</div>
